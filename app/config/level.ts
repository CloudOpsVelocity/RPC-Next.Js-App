// import { Level } from "level";
// import path from "path";

// // Define the path for the LevelDB database
// const dbPath = path.join(process.cwd(), "static", "builderDB");

// // Create or open the LevelDB store
// const db = new Level("builderDB", { valueEncoding: "json" });
const db ={}
export default db;
